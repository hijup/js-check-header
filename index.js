var validHeaderName = require('./valid-header-name')
var validHeaderValue = require('./valid-header-value')

module.exports = {
  validHeaderName: validHeaderName,
  validHeaderValue: validHeaderValue
}
